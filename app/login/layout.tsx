export const metadata = {
  title: "Sign in — ClearCME",
  description: "Sign in to ClearCME to see your CME compliance map, track hours, and close gaps before renewal.",
};

export default function LoginLayout({ children }: { children: React.ReactNode }) {
  return children;
}
